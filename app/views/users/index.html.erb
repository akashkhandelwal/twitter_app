<h1>Listing all users</h1>

<ul>
	<% @users.each do |user| %>
		<li><%= user.name %> 
			<% if user != current_user %>
				<%= link_to "Follow", controller: 'users', action: 'follow_user', id: user.id, method: :post %></li>
			<% end %>
	<% end %>
</ul>